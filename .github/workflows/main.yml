name: Test

on:
  workflow_dispatch:

jobs:
  build-deploy:
    name: Build and Deploy
    runs-on: [nionic-runner]
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Hello World!"
